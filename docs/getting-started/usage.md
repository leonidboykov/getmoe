Start by creating a new file called `getmoe.yaml` (`getmoe.yml` is also OK). Here is an example configuration just to start with something.

``` yaml
boards:
  yande.re:
```

## Boards


``` yaml
boards:
  board_name:
```

## Something else
